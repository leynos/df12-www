{#
  The Rust-extension capability matrix, checked against the release
  named in config/pages.yaml (`cuprum_version`). Each state is a word first:
  `available`, `integrated`, `inert` (implemented, not integrated),
  `proposed`, `investigating`, and `unreleased`. Revisit every row when the
  pinned release moves; the sources are docs/users-guide.md ("Performance
  extensions"), docs/roadmap.md phases 4, 6, 7, and 8, and ADR-001/ADR-002.
#}
{% set state_labels = {
  'available': 'Available',
  'integrated': 'Integrated',
  'inert': 'Implemented, not integrated',
  'proposed': 'Proposed',
  'investigating': 'Under investigation',
  'unreleased': 'Built, not released',
  'python-only': 'Python only',
} %}
{% set capabilities = [
  {'name': 'Inter-stage pipeline pumping',
   'python': 'available', 'rust': 'integrated',
   'notes': 'Used under auto or rust when the extension imports. splice() on Linux, a read/write loop on macOS. Each hop that cannot be handed over safely runs on the Python pump instead.'},
  {'name': 'Pumping on Windows',
   'python': 'available', 'rust': 'python-only',
   'notes': 'Asyncio’s Proactor pipes use overlapped handles the synchronous native pump cannot safely use, so every hop declines with platform_unsupported.'},
  {'name': 'Capturing stdout and stderr',
   'python': 'available', 'rust': 'inert',
   'notes': 'rust_consume_stream exists and is tested, and tests fail if production code routes through it. Integration waits on a measured 20% wall-time win (roadmap phase 6).'},
  {'name': 'Echo and tee to sinks',
   'python': 'available', 'rust': 'proposed',
   'notes': 'A raw-sink helper for qualifying sinks, to be profiled before it is built (roadmap phase 7).'},
  {'name': 'Line callbacks and line events',
   'python': 'available', 'rust': 'python-only',
   'notes': 'Decoding and per-line dispatch stay in Python whichever backend is selected.'},
  {'name': 'Spawning and orchestrating processes',
   'python': 'available', 'rust': 'investigating',
   'notes': 'A synchronous native subprocess path and native pipeline orchestration are open decisions (roadmap step 8.3). Process management is Python today.'},
  {'name': 'Native wheels',
   'python': 'available', 'rust': 'available',
   'notes': 'Published with ' ~ cuprum_pypi ~ ' for CPython 3.13 on Linux (x86_64, aarch64), macOS (x86_64, arm64), and Windows (x86_64). Every other interpreter installs the pure-Python wheel.'},
] %}
