{#
  Passthrough for the documentation pages. `PageContentGenerator` hard-codes
  this filename, and the twelve templates under `pages/` extend it, so it stays
  as a named seam even though the chrome now lives in `_layout.jinja`.
#}
{% extends "_layout.jinja" %}
