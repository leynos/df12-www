{
  // A solution file: it holds no sources of its own and only points editors
  // and `tsc -b` at the two real projects. `bun run typecheck:js` runs each
  // project explicitly rather than building this file, so the gate names
  // the project a failure came from.
  "files": [],
  "references": [{ "path": "./tsconfig.browser.json" }, { "path": "./tsconfig.scripts.json" }]
}
